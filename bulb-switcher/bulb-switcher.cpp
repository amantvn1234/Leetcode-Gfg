class Solution {
public:
    int bulbSwitch(int n) {
        int i=0;
        // while(i*i<=n) // perfect square has odd flips so it became on
        // i++;
        //return i-1;
        return sqrt(n);
         
    }
};